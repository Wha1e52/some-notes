const date = new Date();

date.getFullYear() // год
date.getMonth() // месяц (0-11)
date.getDate() // день месяца (1-31)
date.getHours() // часы (0-23)
date.getMinutes() // минуты (0-59)
date.getSeconds() // секунды (0-59)