"""
Массив - это структура данных, которая представляет собой упорядоченный набор элементов одного типа.

Все элементы статического массива должны иметь единый тип данных.

Длина статического массива (то есть, число его элементов) задается фиксированным значением и не меняется.

Все элементы статического массива располагаются в памяти компьютера последовательно друг за другом.

Вычислительная сложность:
запись O(1)
чтение O(1)
вставка O(n)
удаление O(n)

"""